spring:
  datasource:
    url: jdbc:h2:mem:unit_test
    username: admin
    password: 123456
    driver-class-name: org.h2.Driver

  # h2 页面控制台，可以选择性添加
  h2:
    console:
      enabled: true
      path: /h2-console